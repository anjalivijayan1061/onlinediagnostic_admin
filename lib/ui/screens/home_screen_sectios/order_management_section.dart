import 'package:flutter/material.dart';

class OrderManagmentSection extends StatelessWidget {
  const OrderManagmentSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: SizedBox(
        width: 1000,
        child: ListView(
          children: [],
        ),
      ),
    );
  }
}
