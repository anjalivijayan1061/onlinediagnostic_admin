import 'package:flutter/material.dart';

class TestManagmentSection extends StatelessWidget {
  const TestManagmentSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: SizedBox(
        width: 1000,
        child: ListView(
          children: [],
        ),
      ),
    );
  }
}
